# ─────────────────────────────────────────────
#  Hinglish Tutor Bot — Python Dependencies
#  Install with: pip install -r requirements.txt
# ─────────────────────────────────────────────

gunicorn>=21.2.0       # Production WSGI server (required for Railway/Render)
flask>=3.0.0          # Web framework & template engine
flask-cors>=4.0.0     # Allow browser → Flask CORS requests
requests>=2.31.0      # HTTP calls
gtts>=2.5.0           # Google Text-to-Speech — real Indian female voice
google-genai>=1.0.0   # Gemini AI brain (set GEMINI_API_KEY to enable)
python-dotenv>=1.0.0  # Auto-loads .env file so you never type export again

# ── v6 additions ──────────────────────────────
# difflib  → already in Python stdlib (no install needed) — used for pronunciation fuzzy matching
# sqlite3  → already in Python stdlib (no install needed) — used for progress tracking

# ── CNN additions (optional — app works without these) ─────────────────────────────
# Install for deep learning features:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   pip install librosa numpy soundfile
#
# torch>=2.0.0         # TextCNN proficiency + MelCNN pronunciation + ConfidenceCNN
# librosa>=0.10.0      # Mel-spectrogram & MFCC extraction from audio
# numpy>=1.24.0        # Array ops for audio processing
# soundfile>=0.12.0    # Audio file I/O (WAV/FLAC support for librosa)
